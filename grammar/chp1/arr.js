const array = [1,2,3,4,5]; //숫자 배열

const objects = [{name:'멍멍이'}, {name:'고양이'}]; //객체 배열

console.log(objects);
console.log(objects[0]);
console.log(objects[1]);

objects.push({
    name : "똥개"
});
console.log(objects)

for (let i = 0; i <10 ; i++) {
    console.log(i);
}

